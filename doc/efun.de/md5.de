SYNOPSIS
        string md5(string arg)

BESCHREIBUNG
        Berechnet den MD5-Hashwert des Strings <arg>.
        Das Ergebnis wird als 32-stelliger Hexadezimalwert geliefert.

BEISPIEL
        string s;

        s = md5("Hallo, Welt!");

AENDERUNGEN
        Eingefuehrt in LDMud 3.2.9

SIEHE AUCH
        crypt(E), md5_crypt(E), sha1(E)

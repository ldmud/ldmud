Short: Introduce 'regexp' LPC datatype
From: Lars
Date: 2002-03-28
Type: Feature
State: New

Add an svalue 'regexp' so that wizards can deliberately precompile
regexps. regexp() would then have the signature

  regexp(mixed *, string|regexp, int default: F_CONST0)


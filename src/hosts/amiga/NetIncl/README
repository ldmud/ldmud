The files in this directory hold collections of option settings, used
when configuring the driver for compilation. To use the setting <foo>,
start the configure script with argument --with-<foo>

The provided settings:

  default:  an example file, documenting all available options
  compat:   settings used for a generic compat-mode driver
  native:   settings used for a generic native-mode driver
  osb:      settings used for the mud OSB
  unitopia: settings used for the mud UNItopia


From: "Gnomi" <gnomi@UNItopia.rus.uni-stuttgart.de>
Short: symbol_function on foreach() fails
Date: Mon, 28 Feb 2000 19:20:04 +0100
Type: Bug
State: Done - fixed in 3.2.8-dev.175.

Im Zusammenhang mit foreach gibt es ausserdem einen Bug mit symbol_function:
symbol_function("foreach") muesste eigentlich #'foreach liefern, liefert
stattdessen nur 0. (symbol_function("while") liefert dagegen #'while.)

Analysis:

closure::symbol_efun() simply didn't know about #'foreach.


Date: Wed, 23 Feb 2000 09:13:20 -0700
From: Lars Duening <Lars.Duening@wcom.com>
Short: LDMUD foreach idea
Type: Feature
State: New (implemented in 3.3.44)

foreach (i : <int-expr>) : loops from i = 0 to i = <int-expr>-1
foreach (i : <int1> .. <int2>) : loops from i = <int1> to i = <int2>,
  incrementing or decrementing i depending on range limits

foreach (<expr>), foreach(<int1>..<int2>): loops as usual, but does
not assign the loop values to any variable.

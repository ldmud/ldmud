SYNOPSIS
	int clonep(object obj)

DESCRIPTION
	If <obj> is a clone, the efun returns 1, else it returns 0.
        
	Objects with a replaced program count as clones.

EXAMPLE
	object o;
	o = clone_object("/std/thing");
	write(clonep(o));                   --> writes "/std/thing"
	write(find_object("/std/thing"));   --> writes "0"

HISTORY
	Introduced in LDMud 3.2.6.
        
SEE ALSO
	blueprint(E), clone_object(E)

This directory holds know bugs, patches and feature requests, one per file.

The filenames follow the pattern 'f-yymmdd' for features, 'b-yymmdd' for bugs,
'p-yymmdd' for contributed patches, and 'd?-yymmdd' for everything completed.
<yymmdd> is the date when the original issue was raised.
